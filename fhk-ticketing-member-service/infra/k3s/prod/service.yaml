apiVersion: v1
kind: Service
metadata:
  name: fhk-ticketing-member-service
  namespace: prod
  labels:
    app: fhk-ticketing-member-service
    env: prod
spec:
  type: ClusterIP
  selector:
    app: fhk-ticketing-member-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
