apiVersion: v1
kind: Service
metadata:
  name: fhk-ticketing-movie-service
  namespace: prod
  labels:
    app: fhk-ticketing-movie-service
    env: prod
spec:
  type: ClusterIP
  selector:
    app: fhk-ticketing-movie-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
