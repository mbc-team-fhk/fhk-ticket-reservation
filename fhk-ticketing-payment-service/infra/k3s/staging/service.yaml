apiVersion: v1
kind: Service
metadata:
  name: fhk-ticketing-payment-service
  namespace: staging
  labels:
    app: fhk-ticketing-payment-service
    env: staging
spec:
  type: ClusterIP
  selector:
    app: fhk-ticketing-payment-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
