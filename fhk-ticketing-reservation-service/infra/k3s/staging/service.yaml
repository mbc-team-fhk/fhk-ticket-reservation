apiVersion: v1
kind: Service
metadata:
  name: fhk-ticketing-reservation-service
  namespace: staging
  labels:
    app: fhk-ticketing-reservation-service
    env: staging
spec:
  type: ClusterIP
  selector:
    app: fhk-ticketing-reservation-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
