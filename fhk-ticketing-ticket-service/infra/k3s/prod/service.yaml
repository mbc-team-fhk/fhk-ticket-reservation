apiVersion: v1
kind: Service
metadata:
  name: fhk-ticketing-ticket-service
  namespace: prod
  labels:
    app: fhk-ticketing-ticket-service
    env: prod
spec:
  type: ClusterIP
  selector:
    app: fhk-ticketing-ticket-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
