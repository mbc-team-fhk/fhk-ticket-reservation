apiVersion: v1
kind: Service
metadata:
  name: fhk-ticketing-ticket-service
  namespace: staging
  labels:
    app: fhk-ticketing-ticket-service
    env: staging
spec:
  type: ClusterIP
  selector:
    app: fhk-ticketing-ticket-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
